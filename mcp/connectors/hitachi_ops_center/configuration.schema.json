{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "urn:project-atlas:connector:hitachi-ops-center:configuration:0.1.0",
  "title": "Hitachi Ops Center Configuration Manager connector configuration",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "endpoint_host",
    "endpoint_port",
    "certificate_trust_reference_id",
    "credential_reference_id",
    "allowed_storage_device_ids"
  ],
  "properties": {
    "endpoint_host": {
      "type": "string",
      "minLength": 1,
      "maxLength": 253,
      "pattern": "^[A-Za-z0-9](?:[A-Za-z0-9.-]{0,251}[A-Za-z0-9])?$"
    },
    "endpoint_port": {
      "type": "integer",
      "minimum": 1,
      "maximum": 65535,
      "default": 23451
    },
    "certificate_trust_reference_id": {
      "type": "string",
      "pattern": "^trust\\.[a-z0-9_.:-]{3,127}$"
    },
    "credential_reference_id": {
      "type": "string",
      "pattern": "^secret\\.[a-z0-9_.:-]{3,127}$"
    },
    "allowed_storage_device_ids": {
      "type": "array",
      "minItems": 1,
      "maxItems": 500,
      "uniqueItems": true,
      "items": {
        "type": "string",
        "pattern": "^[A-Za-z0-9]{6,32}$"
      }
    }
  }
}
